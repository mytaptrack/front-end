export enum DateSelection {
    week = 'week',
    month = 'month',
    year = 'year'
}

export enum DurationStatus {
    None,
    Start,
    Stop
}

export enum ReportType {
    Data = 'Data',
    Frequency = 'Frequency'
}
export enum DurationUnits {
    seconds = 'Seconds',
    minutes = 'Minutes',
    hours = 'Hours'
}